import React, {Component} from 'react'
import classes from './StartPage'

class StartPage extends Component {
    render() {
        return(
            <div className = {classes.StartPage}>
                <h1> Start page </h1>
            </div>
        )
    }
}

export default StartPage